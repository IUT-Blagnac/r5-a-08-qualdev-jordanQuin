
const { Given, When, Then } = require('cucumber');

Given('I have {int} cucumbers in my basket', (cucumbers) => {
  // Implementation code
});

When('I eat {int} cucumbers', (cucumbers) => {
  // Implementation code
});

Then('I should have {int} cucumbers left', (remainingCucumbers) => {
  // Implementation code
});
